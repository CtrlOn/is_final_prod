1. gravity is broken:
    using the slider continuosly causes unconnected pieces to drift away to infinity
    doesnt seem to cause pull towards center
^ fixed

2. dont expose physics sliders to user. hardcode at -500, 150, ??? (whatever causes other particles to not drift away)
^ fixed

3. pubs are doubled?
^ fixed

4. the beachball effect thing, make it.
^fixed

5. persistent blank hovering info box
^ TODO:

6. hovering info box (the one thats working) has limited width causing ugly wrapping, extend the width limit
^ fixed

7. dont hardcode anything about institutes, get the names from txt filenames and colors by tab4 or smth
^ fixed

8. On node click/select camera flies too close to it
^ TODO: